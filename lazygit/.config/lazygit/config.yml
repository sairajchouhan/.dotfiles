git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never

os:
  editCommand: 'nvim'
  editCommandTemplate: '{{editor}} +{{line}} -- {{filename}}'
  # openCommand: 'nvim {{filename}}'

notARepository: 'quit'
